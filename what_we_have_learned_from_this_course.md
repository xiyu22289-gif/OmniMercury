# Summer RSS Reader 本地跨平台AI阅读器
## 小组项目汇报文档
第三组：王梓越，唐语，王乐昆
汇报日期：2026年7月29日

## 目录
1. [项目简介](#1-项目简介)
2. [开发环境与AI工具链](#2-开发环境与ai工具链)
3. [开发过程问题、故障与完整解决方案](#3-开发过程问题故障与完整解决方案)
4. [项目踩坑总结与开发经验](#4-项目踩坑总结与开发经验)
5. [AI辅助编程深度心得体会](#5-ai辅助编程深度心得体会)
6. [项目未来迭代展望](#6-项目未来迭代展望)

---

# 1. 项目简介
## 项目定位
**本地优先、跨平台 AI RSS 阅读器**
对标商业级产品 Mercury，全程本地存储所有数据，无云端上传，彻底规避用户隐私泄露风险。

## 核心技术内容
- 完整工程化 Electron 三进程架构
- 自研标准化文章内容清洗流水线
- 通用中立大模型接入层，全兼容 OpenAI 系列模型
- 全套标准化工程规范文档：`INIT.md` / `PLAN.md` / `AGENTS.md`

---

# 2. 开发环境与AI工具链
## 2.1 基础开发环境 & 技术栈亮点
- Electron 标准化工程化开发体系
- 本地 SQLite 轻量化数据库存储
- 原生桌面客户端应用开发

### 跨平台打包部署
- 同时支持 Windows / macOS 双端安装包发布
- 基于 GitHub Actions 搭建全自动 CI/CD 流水线

## 2.2 全套AI开发工具链
### ① VSCode 内置主力工具：Cline / ClaudeCode（最高频）
接入模型：DeepSeek V4 Pro
核心用途：
- 代码重构、IPC通信接口编写、页面组件批量生成
- 自动读取项目 `AGENTS.md` 规范，约束代码统一架构标准

### ② GitHub Copilot（低频辅助）
核心用途：短片段代码自动补全、简易Bug临时快速排查

### ③ 网页端通用对话AI：DeepSeek
核心用途：
- 项目整体架构设计（使用频次最高）
- 维护 `PLAN.md` / `INIT.md` / `AGENTS.md` 全套项目文档
- 报错日志解析、技术资料检索、界面配色方案设计

---

# 3. 开发过程问题、故障与完整解决方案
分为**个人独立开发问题**、**团队协作合并问题**两大模块，按开发时序整理全部故障案例。

## 3.1 个人独立开发阶段问题
### 问题1：需求拆分粒度粗糙，AI生成大量不可调试Bug
- 现象：顶层需求未拆细分模块、指令描述模糊，LLM批量生成代码存在连锁故障
- 解决方案：将功能拆解至最小可运行单元；使用DeepSeek优化需求描述措辞

### 问题2：编码仅正向测试，缺失异常捕获逻辑
- 现象：仅校验正常样例，忽略边界报错场景，异常触发直接程序崩溃
- 解决方案：查阅资料+向授课老师确认模块底层运行原理；提前预判异常分支，补充try-catch容错逻辑，并更新至`Agents.md`规范文档

### 问题3：多模块集成异步并发请求数据错乱（页面穿模）
- 现象：单模块单独测试正常，页面快速切换后并发异步请求覆盖数据，展示错乱
- 解决方案：增加请求任务唯一标识、过期请求过滤、主动取消闲置请求；新增并发专项测试流程

### 问题4：AI重复编写通用逻辑，无公共代码复用意识
- 现象：新增功能时AI重复实现已有工具方法，代码冗余、模块耦合度升高
- 解决方案：向AI明确指定项目现有公共工具文件，强制复用已有封装代码

## 3.2 团队多人协作开发问题
### 问题1：分支合并出现海量代码冲突，衍生逻辑Bug
- 现象：多成员并行开发后合并分支，大量文件冲突，修复难度极高
- 解决方案：减少工具自动合并；让AI标记全部冲突点位，小组统一审核后制定标准化修复方案

### 问题2：新增功能互相干扰，跨模块隐性冲突
- 现象：独立开发无异常，集成后不同功能互相抢占状态、快捷键冲突
- 解决方案：新增功能开发前全组同步需求；严格划定各模块功能边界，统一快捷键、全局状态规范

## 3.3 项目完整故障案例（11类典型踩坑实录）
### 故障1：Git推送仓库失败，单包体积190MB超时断开
1. 现象：首次`git push`报RPC failed、HTTP 408超时，远程仓库强制断开连接
2. 根因：AI自动生成的`.gitignore`规则不全，`node_modules`、`dist`构建产物未屏蔽，大文件提交入库
3. 修复方案：使用`git rm -r --cached`清理Git缓存内大文件夹；完善忽略规则；切换SSH协议稳定推送
4. 经验：初始化项目必须人工校验`.gitignore`，依赖包、构建产物严禁提交仓库

### 故障2：jsdom ESM模块不兼容，应用启动直接崩溃
1. 现象：执行`npm run dev`启动项目，抛出`ERR_REQUIRE_ESM`报错
2. 根因：新版jsdom依赖纯ESM模块`html-encoding-sniffer`，Electron主进程为CommonJS环境无法加载
3. 修复方案：降级jsdom至22.1.0稳定兼容版本
4. 经验：AI默认拉取最新依赖，新版本极易存在环境兼容问题，引入包前校验版本适配性

### 故障3：better-sqlite3在GitHub Actions CI编译失败
1. 现象：本地Windows打包正常，自动化CI构建时报缺少distutils模块
2. 根因：CI默认Python3.14移除distutils，node-gyp编译原生数据库模块依赖该库；Node版本与本地不一致
3. 修复方案：CI配置文件锁定Node 20、Python3.11；关闭macOS自动代码签名
4. 经验：本地运行正常不代表CI环境兼容，流水线必须固定依赖版本，踩坑记录存入`AGENTS.md`

### 故障4：表单按钮触发页面自动刷新，新增订阅源丢失
1. 现象：首次添加RSS源正常，第二次添加页面刷新，数据直接清空
2. 根因：button未声明`type="button"`，浏览器默认识别为表单提交按钮；回车事件未阻止默认行为
3. 修复方案：统一补充按钮类型；键盘事件添加`preventDefault()`
4. 经验：AI生成前端UI代码容易忽略基础Web规范，页面功能必须完整多场景测试

### 故障5：翻译缓存持久化失效，大模型API调用量暴涨
1. 现象：数据库存储翻译缓存，但前端读取直接丢弃，每次打开文章重复调用LLM
2. 根因：后端写入缓存版本号`_v=3`，前端读取逻辑仅接受`_v=2`，版本校验不匹配直接清空缓存
3. 修复方案：统一全项目缓存版本号定义，同步前后端校验规则
4. 经验：向AI反馈故障必须提供**日志现象+预期结果**，模糊描述会大幅降低排错效率

### 故障6：团队成员强制推送Git分支，重写远程历史
1. 现象：组员执行`git push --force`覆盖main分支历史，本地拉取报无关历史合并错误，40+文件冲突
2. 修复方案：本地完整备份代码，重新克隆纯净远程仓库，手动复制业务代码逐行核对合并
3. 团队规范：非极端紧急场景，全面禁止强制推送代码

### 故障7：多功能分支合并，15个核心文件大规模冲突
1. 现象：合并搜索、快捷键、繁体翻译分支后，`package.json`、数据库、IPC处理文件全部冲突
2. 修复方案：小组提前约定冲突取舍规则；分工处理冲突文件，借助Claude Code辅助代码合并
3. 经验：合并前统一功能保留标准，核心文件单人集中处理，避免多人并行修改同一文件

### 故障8：LLM翻译占位符还原失败，LINK/IMG标识直接展示在译文
1. 现象：超链接、图片占位符被大模型修改格式，还原匹配逻辑失效，问题合并后重复复现
2. 根因：LLM输出不可控，会自动修改占位符格式（加反引号、空格、加粗）
3. 修复方案：增强还原函数，新增多套模糊匹配规则兼容LLM各类输出变形
4. 经验：与大模型交互逻辑必须预留容错处理，完整打印原始响应日志用于定位故障

### 故障9：双套深浅色主题状态冲突，切换不同步
1. 现象：阅读区独立主题按钮与全局深色模式两套状态，界面配色割裂；仅删除按钮无法解决
2. 修复方案：彻底移除阅读区独立主题全部状态、常量、样式代码，统一复用全局深色模式变量
3. 经验：UI控件删除不等于底层状态清理，状态冲突需从数据源头完整移除冗余逻辑

### 故障10：项目收尾批量功能失效，未做回归测试
1. 现象：侧边栏宽度、主题存储、文章排序等历史功能重启重置、展示异常
2. 根因：迭代合并冲突隐性破坏旧逻辑；SQL查询未处理空值排序
3. 修复方案：编写109项完整功能测试清单逐项校验；使用`COALESCE`兼容SQL空值场景
4. 经验：项目收尾必须全量回归测试，不能仅验证新增功能

---

# 4. 项目踩坑总结与开发经验
## 4.1 AI辅助编码经验
清晰的前期规划、代码复用规范、风险预判与容错架构，必须由开发者人工完成，不能完全依赖AI自动实现。

## 4.2 模块集成架构经验
1. 异步时序管控、数据隔离等底层架构防护逻辑，只能人工设计定义；
2. 全局功能风险、业务逻辑取舍、跨模块冲突判断，必须全团队同步对齐。

## 4.3 版本协作管控经验
1. 严禁无约束强制推送Git分支，提前统一团队Git操作规范；
2. 多分支合并前确定功能保留规则，降低冲突修复成本；
3. CI/CD环境与本地开发环境分开配置，固定所有依赖版本。

## 4.4 前端与大模型交互经验
1. AI生成代码存在大量基础规范漏洞，页面、接口、缓存功能必须多场景测试；
2. LLM输出具备不稳定性，所有解析、还原逻辑设计多层容错匹配；
3. 数据库存储、版本校验、状态持久化逻辑前后端统一定义，避免校验断层。

---

# 5. AI辅助编程深度心得体会
## 核心认知1：AI仅擅长局部单次代码生成，缺失全局工程思维
人工智能无法理解完整项目业务链路、长期统一代码规范、多模块联动交互逻辑；人类开发者才能把控整体架构、预判边界异常、统筹团队协作问题。

## 核心认知2：AI仅能作为代码辅助工具，项目核心环节无法替代
1. 工程整体架构必须人工设计；
2. 复杂底层故障、跨模块冲突问题需要人工排查处理；
3. 完整项目进度、功能优先级、团队分工需要人类统筹规划。
> 无论AI代码生成能力如何迭代，人类全局思考、逻辑梳理、权衡决策的核心能力始终不可替代。

## 四大核心开发方法论总结
### ① 人是项目决策者，AI为执行工具
AI仅能罗列技术方案、快速编写代码，但技术选型、功能优先级、方案取舍、架构红线全部需要人工判断划定。AI如同初级开发工程师，依赖人类清晰指令与顶层约束。

### ② 文档驱动开发是高效协作AI的基础
本项目完整遵循文档驱动流程：
1. 前期人工编写`INIT.md`：确定项目目标、约束、核心功能；
2. 和AI共同完善技术选型方案；
3. 生成`AGENTS.md`固化编码规范、历史故障、架构规则；
4. 输出`PLAN.md`拆分开发里程碑与分步任务。
统一文档作为AI的项目记忆库，避免AI偏离项目规范，重复踩同类漏洞。

### ③ 精准描述问题是和AI协作的核心能力
模糊描述（“缓存不好用”）与标准化描述（“日志显示_v=3，前端逻辑_v!==2直接丢弃缓存”）修复效率差距极大。
固定故障描述结构：**现象 + 日志原始信息 + 预期正常结果**，精准信息可让AI快速定位根因，减少无效调试。

### ④ 测试与代码审查是项目质量最终防线
1. AI生成代码表面可运行，但极易隐藏边界逻辑缺陷，完整测试必不可少；
2. AI会复制项目现有代码风格，劣质代码、冗余写法会持续扩散；
3. 代码重构、代码质量优化只能由人工主动推动，AI不会自发整理冗余代码。

---

# 6. 项目未来迭代展望
1. **多平台安装包发布至GitHub Releases**
当前Windows/macOS/Linux安装包已构建完成，上传发布页面开放公众下载使用。

2. **优化长文章加载渲染性能**
针对超长资讯文章实现分页、按需懒加载，缩短页面首屏加载等待时间。

3. **新增用户反馈入口**
客户端内置反馈提交模块，收集真实使用场景Bug与功能需求，持续迭代优化。

4. **拓展多格式数据导出功能**
现有笔记仅支持Markdown导出，后续新增HTML、PDF导出；完善全量订阅源、标签、笔记数据备份迁移功能。

5. **长期持续维护更新**
定期迭代新版本，优先修复用户反馈故障，持续完善功能，打造稳定可用的本地AI RSS阅读器——Summer RSS Reader。

---

## 致谢
感谢各位老师、同学聆听小组项目汇报！
第三组：王梓越，唐语，王乐昆
2026年7月29日